# 项目依赖清单 (requirements.txt)

# 核心框架
torch>=1.10.0          # PyTorch深度学习框架，用于模型定义、训练和张量运算

# 数据可视化
matplotlib>=3.4.0      # 用于绘制散点图、损失曲线等可视化图表

# 说明：
# 1. 安装命令：pip install -r requirements.txt
# 2. 版本号使用>=确保兼容性，可根据实际环境调整
# 3. 本项目基于PyTorch实现线性回归与深度回归模型，依赖核心框架与可视化库，无额外特殊依赖